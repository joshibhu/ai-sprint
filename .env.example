# Template — committed. Copy to .env and fill in real values.
OPENAI_API_KEY=

# Read-only login. NEVER sprint_owner — every guard above depends on this.
DATABASE_URL=postgresql://sprint_app:app_dev_password@localhost:5433/sprint
